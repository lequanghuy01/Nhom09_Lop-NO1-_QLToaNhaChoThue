import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(
    0xFF111827,
  ); // Nền xanh đen tối
  static const Color cardBackground = Color(
    0xFF1E293B,
  ); // Nền của form đăng nhập
  static const Color primaryBlue = Color(
    0xFF1D4ED8,
  ); // Màu xanh dương chủ đạo
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(
    0xFF94A3B8,
  );
  static const Color inputBackground = Color(
    0xFF0F172A,
  ); // Nền ô input
  static const Color borderColor = Color(
    0xFF334155,
  );
  static const Color successGreen = Color(
    0xFF10B981,
  ); // Xanh lá
  static const Color warningOrange = Color(
    0xFFF59E0B,
  ); // Cam
  static const Color purpleAlert = Color(
    0xFF8B5CF6,
  ); // Tím
  static const Color surfaceLight = Color(
    0xFF1E293B,
  ); // Màu nền của các thẻ
  static const Color dividerColor = Color(
    0xFF334155,
  );
  static const Color errorRed = Color(
    0xFFEF4444,
  ); // Đỏ cho trạng thái Hết hạn/Quá hạn
}
